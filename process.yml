apps:
  - script   : index.js
    name: real-chat
    instances: max - 1
    exec_mode: cluster
    watch  : true
    env    :
      NODE_ENV: development
    env_production:
      NODE_ENV: production
